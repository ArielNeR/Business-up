<xct:Popup
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit" 
    xmlns:visual="clr-namespace:Business_Up.Visual" x:DataType="visual:TipoBalance"
    x:Class="Business_Up.Visual.TipoBalance"
    Color="Transparent">
    <Frame CornerRadius="30" Margin="40,100,100,40">
        <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Button x:Name="Diario" Text="Diario" Grid.Row="0" HorizontalOptions="Center" BackgroundColor="Transparent" Clicked="Diario_Clicked"/>
                <Button x:Name="Semanal" Text="Semanal" Grid.Row="1" HorizontalOptions="Center" BackgroundColor="Transparent" Clicked="Semanal_Clicked"/>
                <Button x:Name="Mensual" Text="Mensual" Grid.Row="2" HorizontalOptions="Center" BackgroundColor="Transparent" Clicked="Mensual_Clicked"/>
                <Button x:Name="Anual" Text="Anual" Grid.Row="3" HorizontalOptions="Center" BackgroundColor="Transparent" Clicked="Anual_Clicked"/>

            </Grid>
    </Frame>
</xct:Popup>